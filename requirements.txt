# Core Dependencies
torch>=1.13.0       # Or specific version compatible with your hardware/CUDA
TTS>=0.22.0         # Coqui TTS library (check latest recommended version)
Flask>=2.0          # Web framework
simpleaudio>=1.0.4  # Audio playback for GUI
numpy>=1.20.0       # Numerical processing
soundfile>=0.11.0   # Reading/writing audio files
Pillow>=9.0.0       # Image processing for GUI/Utils
requests>=2.25.0    # For making API requests (SD, potentially others)
ollama>=0.1.6       # Ollama client library (check latest)

# Optional/Development
# watchdog          # For potential auto-refresh features (if added later)